# Figma макет  
https://www.figma.com/design/044Vxpew9G847tHeLmAMek/Quote-Machine-Generator--Community-?node-id=0-1&t=ruBuM6ymIhxuftFs-1
---

# 💬 Quote Generator  
Небольшое веб-приложение для генерации случайных цитат. Интерфейс выполнен в стильной карточке поверх фонового изображения, а запросы отправляются к API через прокси, чтобы приложение стабильно работало на GitHub Pages.

---

## 🚀 Возможности

- Получение случайной цитаты  
- Показ автора цитаты  
- Обновление цитаты по нажатию кнопки  
- Работает на GitHub Pages без CORS ошибок  
- Лёгкий адаптив под мобильные устройства  
- Минималистичный современный UI  

---

## 🛠 Технологии

- **HTML** — структура приложения  
- **CSS** — оформление карточки, фона и адаптив  
- **JavaScript** — логика загрузки цитат и обработчик событий  
- **Quotable API** — источник цитат  
- **AllOrigins CORS Proxy** — позволяет работать с API на GitHub Pages  

---

## 📦 Установка и запуск

1. Скачайте или клонируйте проект  
   ```bash
   git clone https://github.com/your-name/quote-generator.git
   ```

2. Перейдите в папку проекта  
   ```bash
   cd quote-generator
   ```

3. Откройте `index.html`  
   * двойным кликом  
   * или через локальный сервер:

   ```bash
   npx serve .
   ```

   или

   ```bash
   python -m http.server 5500
   ```

---

## 📂 Структура проекта

```
quote-generator/
│── index.html
│── styles.css
│── script.js
│── assets/
│    └── background.jpg
└── README.md
```

---

## 🧠 Как это работает

Приложение использует бесплатный API **Quotable**, но так как GitHub Pages блокирует прямые запросы, используется прокси:

```js
const proxyUrl = "https://api.allorigins.win/get?url=" + encodeURIComponent("https://api.quotable.io/random");
```

Загрузка цитаты:

```js
const wrapper = await fetch(proxyUrl).then(r => r.json());
const data = JSON.parse(wrapper.contents);

quoteEl.textContent = data.content;
authorEl.textContent = data.author;
```

Пользователь может получить новую цитату по нажатию кнопки, которая вызывает `fetchQuote()`.

---

## 📜 Лицензия

Свободно используйте, модифицируйте и улучшайте приложение.
